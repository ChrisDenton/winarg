[workspace]

members = [
    "winarg",
    "testing"
]

# Enable some optimizations for tests seeing as it can take awhile with a larger data set.
[profile.test]
opt-level = 3
lto = "thin"
incremental = false
codegen-units = 1
